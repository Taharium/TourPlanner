using System.Windows;

namespace Tour_Planner.ViewModels {
    public class EditTourLogWindowVM : ViewModelBase {
        private readonly Window _window;

        public EditTourLogWindowVM(Window window) {
            _window = window;

        }
    }
}
